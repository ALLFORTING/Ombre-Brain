name: Tests

on:
  push:
    branches: [main]
    paths-ignore:
      - '*.md'
      - 'backup_*/**'
      - '.gitignore'
  pull_request:
    branches: [main]
  workflow_dispatch:
  schedule:
    - cron: '17 3 * * 1'

jobs:
  viewer-js:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ui/asset-viewer
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: ui/asset-viewer/package-lock.json

      - name: Install viewer dependencies
        run: npm ci

      - name: Run Viewer JavaScript tests
        run: npm test
  offline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12.14
        uses: actions/setup-python@v5
        with:
          python-version: '3.12.14'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt -c constraints-py312-linux.txt
          pip install pytest pytest-asyncio -c constraints-py312-linux.txt

      - name: Prepare isolated buckets directory
        run: mkdir -p "${{ runner.temp }}/ombre-buckets"

      - name: Run offline tests and security regressions
        env:
          OMBRE_BUCKETS_DIR: ${{ runner.temp }}/ombre-buckets
        run: python -m pytest -m "not external" -v --asyncio-mode=auto

  external:
    if: ${{ github.event_name == 'workflow_dispatch' || github.event_name == 'schedule' }}
    runs-on: ubuntu-latest
    env:
      OMBRE_API_KEY: ${{ secrets.OMBRE_API_KEY }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12.14
        uses: actions/setup-python@v5
        with:
          python-version: '3.12.14'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt -c constraints-py312-linux.txt
          pip install pytest pytest-asyncio -c constraints-py312-linux.txt

      - name: Prepare isolated buckets directory
        run: mkdir -p "${{ runner.temp }}/ombre-buckets"

      - name: Run external quality tests
        if: env.OMBRE_API_KEY != ''
        env:
          OMBRE_BUCKETS_DIR: ${{ runner.temp }}/ombre-buckets
        run: python -m pytest -m external -v --asyncio-mode=auto

      - name: Report external tests not configured
        if: env.OMBRE_API_KEY == ''
        run: echo "OMBRE_API_KEY is not configured; external tests were not run."
