# ============================================================
# Ombre Brain Python 依赖
# 安装: pip install -r requirements.txt
# ============================================================

# MCP 协议 SDK（Claude 通信核心）
mcp>=1.27,<2
python-multipart>=0.0.32,<1

# 模糊匹配（记忆桶搜索）
rapidfuzz>=3.0.0

# OpenAI 兼容客户端（支持 DeepSeek/Ollama/LM Studio/vLLM/Gemini 等任意兼容 API）
openai>=1.0.0

# YAML 配置解析
pyyaml>=6.0

# Markdown frontmatter 解析（桶文件读写）
python-frontmatter>=1.1.0

# Remember-Me image decoding and metadata stripping
Pillow>=11,<13

# Public Remember-Me Core pinned to an immutable source archive.
remember-me @ https://github.com/peanutsuee/Remember-Me/releases/download/v0.1.0-dev.7-public.1/Remember-Me-0.1.0.dev7-public.1-a00ea991442d7581a3856b178525a8e77da833fe.tar.gz#sha256=80a0b334f08db19c95c053537dec484be645f29fcf67898037e6641224012214

# 中文分词
jieba>=0.42.1

# 异步 HTTP 客户端（应用层保活 ping）
httpx>=0.27.0
PyJWT[crypto]>=2.8.0
# Offline backup bundle envelope encryption (direct dependency).
cryptography>=45,<50

# 向量相似度计算 (导入模式/聚类)
numpy>=1.24.0
scikit-learn>=1.2.0
